<div class="container">
  <div class="index-container">

    <div class="app-header">
      <div class="app-title">
        <h1>Appointments</h1>
      </div>
    </div>

    <% @appointments.each do |appointment| %>
      <div class="content">
        <p><%= appointment.client.name %> on <%= appointment.date %></p>
      </div>
    <% end %>

    <%= link_to 'Back to clients list', clients_path %>

  </div>
</div>
